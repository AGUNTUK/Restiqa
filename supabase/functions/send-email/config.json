{
  "name": "send-email",
  "version": "1.0.0",
  "description": "Send emails via Resend or SendGrid",
  "environment": {
    "RESEND_API_KEY": {
      "description": "Resend API key for sending emails"
    },
    "SENDGRID_API_KEY": {
      "description": "SendGrid API key for sending emails"
    }
  }
}
